# Local Build Configuration
# Used by both local and GitHub Actions builds

# Device Configuration
DEVICE_ID=glinet_gl-mt6000
OPENWRT_VERSION=24.10.5
TOLLGATE_VERSION=v0.0.1
RELEASE_CHANNEL=stable

# GitHub Configuration (for GitHub CLI builds)
REPO=jpgaviria/tollgate-os
OUTPUT_DIR=./firmware

# Build Directory (for local Docker builds)
BUILD_DIR=/tmp/tollgate-local-build

# Nostr Keys (for future Blossom upload, optional for local build)
# NOSTR_SECRET_KEY=your_hex_private_key
# NOSTR_PUBLIC_KEY=your_hex_public_key
# NSEC=your_nsec
# NSEC_BECH32=your_nsec_bech32
